{
  "name": "vindb-web",
  "version": "0.0.0",
  "scripts": {
    "postinstall": "npm install --only=dev --prefix server && npm install --prefix server && npm install --only=dev --prefix client && npm install --prefix client"
  }
}
